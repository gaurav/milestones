[project]
name = "milestones"
version = "0.1.0"
description = "Personal cross-repo GitHub milestone manager"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
milestones = "milestones.cli:main"

[dependency-groups]
dev = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
